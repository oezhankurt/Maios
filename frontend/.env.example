# Base URL for the backend API. In dev, Vite proxies /api to the backend,
# so this can be left as /api. For production (Vercel), set the full URL.
VITE_API_BASE_URL=/api
VITE_API_TARGET=http://localhost:5000
